build:
  dependencies:
    override:
      - ls -la config/Shared
      - ls -la src/Generated/Shared/Transfer
  environment:
    php:
      version: 7.1
filter:
  excluded_paths:
    - build/*
    - config/*
    - tests/*
    - src/Pyz/*
    - vendor/*
checks:
  php:
    code_rating: true