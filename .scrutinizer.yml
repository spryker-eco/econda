build:
  dependencies:
    override:
      - ls -la config/Shared
  environment:
    php:
      version: 7.1
filter:
  excluded_paths:
    - build/*
    - config/*
    - tests/*
    - src/Pyz/*
checks:
  php:
    code_rating: true