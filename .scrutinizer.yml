build:
  dependencies:
    before:
      - sed -i -e 's/"Spryker"\:/""\:/g' composer.json
  environment:
    php:
      version: 7.1
filter:
  excluded_paths:
    - build/*
    - config/*
    - tests/*
checks:
  php:
    code_rating: true