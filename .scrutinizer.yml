build:
  dependencies:
    override:
      - cat config/Shared/stores.php
  environment:
    php:
      version: 7.1
filter:
  excluded_paths:
    - build/*
    - config/*
    - tests/*
checks:
  php:
    code_rating: true